function env(n){return String(process.env[n]||'').trim()}
function out(res,s,b){res.status(s).setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');res.end(JSON.stringify(b))}
async function read(r){const t=await r.text();try{return t?JSON.parse(t):{}}catch(_){return{message:t||`HTTP ${r.status}`}}}
function q(v){return encodeURIComponent(String(v==null?'':v))}
function roleOf(v){v=String(v||'').toLowerCase();return v==='direktur'?'direksi':v==='koord'?'koordinator':v==='mgmt'?'management':v}
function canRead(r){return['admin','direksi','management','koordinator'].includes(r)}
function canWrite(r){return['admin','direksi'].includes(r)}
function monthRange(month){const m=/^(\d{4})-(\d{2})$/.exec(String(month||''));if(!m)throw new Error('month wajib YYYY-MM');const y=+m[1],n=+m[2];if(n<1||n>12)throw new Error('month tidak valid');const last=new Date(Date.UTC(y,n,0)).getUTCDate();return{start:`${m[1]}-${m[2]}-01`,end:`${m[1]}-${m[2]}-${String(last).padStart(2,'0')}`,year:y,month:n}}
function monthStart(v){return monthRange(v).start}
async function sbFetch(path,opts={}){const url=env('SUPABASE_URL'),svc=env('SUPABASE_SERVICE_ROLE_KEY');if(!url||!svc)throw new Error('Supabase server env missing');const r=await fetch(url+path,{...opts,headers:{apikey:svc,Authorization:`Bearer ${svc}`,'Content-Type':'application/json',...(opts.headers||{})}});const body=await read(r);if(!r.ok)throw new Error(body.message||body.error||`Supabase HTTP ${r.status}`);return body}
async function actor(req){const url=env('SUPABASE_URL'),pub=env('SUPABASE_PUBLISHABLE_KEY');const bearer=String(req.headers.authorization||'');if(!bearer.startsWith('Bearer '))throw new Error('Session required');const ur=await fetch(`${url}/auth/v1/user`,{headers:{apikey:pub,Authorization:bearer}}),u=await read(ur);if(!ur.ok||!u?.id)throw new Error('Session invalid');const rows=await sbFetch(`/rest/v1/user_profiles?id=eq.${q(u.id)}&select=id,email,full_name,role,staff_id,branch_id,is_active&limit=1`),p=rows?.[0];if(!p?.is_active)throw new Error('Profil tidak aktif');p.role=roleOf(p.role);return{user:u,profile:p,bearer}}
async function actorBranch(p){if(!p.branch_id)return null;const rows=await sbFetch(`/rest/v1/branches?id=eq.${q(p.branch_id)}&select=id,code,name,slug,is_active&limit=1`);return rows?.[0]||null}
async function branches(p){if(!canRead(p.role))throw new Error('Role tidak boleh akses HRIS');if(p.role==='koordinator'){const b=await actorBranch(p);return b?[b]:[]}return await sbFetch('/rest/v1/branches?is_active=eq.true&select=id,code,name,slug,branch_type,is_active&order=name.asc')}
async function employees(req,p){if(!canRead(p.role))throw new Error('Role tidak boleh melihat Karyawan');const company=String(req.query.company_code||'ALL'),status=String(req.query.status||'ALL');let path='/rest/v1/employees?select=*&order=full_name.asc&limit=500';if(company&&company!=='ALL')path+=`&company_code=eq.${q(company)}`;if(status&&status!=='ALL')path+=`&status=eq.${q(status)}`;let rows=await sbFetch(path);if(p.role==='koordinator'){const prof=await sbFetch(`/rest/v1/user_profiles?branch_id=eq.${q(p.branch_id||'')}&is_active=eq.true&select=staff_id`);const allowed=new Set((prof||[]).map(x=>String(x.staff_id||'').toUpperCase()).filter(Boolean));rows=(rows||[]).filter(x=>allowed.has(String(x.employee_id||'').toUpperCase()))}return rows}
async function preactivation(p){if(!['admin','direksi','management'].includes(p.role))throw new Error('Role tidak boleh melihat pre-activation workforce');const rows=await sbFetch('/rest/v1/raos_staff_master?is_activated=eq.false&airport=eq.SOETA&select=staff_id,full_name,email,phone,airport,terminal,role,status,is_activated,branch_id,auth_user_id&order=full_name.asc&limit=500');return{rows:rows||[],summary:{total:(rows||[]).length,active:(rows||[]).filter(x=>x.status==='Aktif').length,inactive:(rows||[]).filter(x=>x.status==='Nonaktif').length,unassigned:(rows||[]).filter(x=>!x.branch_id).length,without_email:(rows||[]).filter(x=>!x.email).length},source:'raos_staff_master',scope:'SOETA_PREACTIVATION',read_only:true}}
async function attendance(req,p){if(!canRead(p.role))throw new Error('Role tidak boleh melihat Absensi');const from=String(req.query.date_from||''),to=String(req.query.date_to||'');if(!/^\d{4}-\d{2}-\d{2}$/.test(from)||!/^\d{4}-\d{2}-\d{2}$/.test(to))throw new Error('date_from/date_to wajib YYYY-MM-DD');let branch=String(req.query.branch_id||'');if(p.role==='koordinator')branch=String(p.branch_id||'');let bf='';if(branch){const br=await sbFetch(`/rest/v1/branches?id=eq.${q(branch)}&select=id,name,slug&limit=1`);if(!br?.[0])return[];bf=`&or=(branch_slug.eq.${q(br[0].slug)},branch_name.eq.${q(br[0].name)})`}const sel='attendance_id,date,staff_id,staff_name,employee_code,company_code,branch_name,branch_slug,shift_name,shift_start_time,effective_check_in,effective_check_out,check_in_at,check_in_at_override,check_out_at,check_out_at_override,is_location_valid,selfie_in_url,selfie_out_url,late_minutes,late_deduction_idr,status,edit_reason';return await sbFetch(`/rest/v1/hris_attendance_view?date=gte.${q(from)}&date=lte.${q(to)}${bf}&select=${sel}&order=date.desc&limit=5000`)}
async function payroll(req,p){if(!canRead(p.role))throw new Error('Role tidak boleh melihat Payroll');const mr=monthRange(req.query.month);let branchId=String(req.query.branch_id||'');if(p.role==='koordinator')branchId=String(p.branch_id||'');const profiles=await sbFetch('/rest/v1/user_profiles?is_active=eq.true&select=id,staff_id,full_name,role,branch_id,gaji,email');const profileByCode=Object.fromEntries((profiles||[]).filter(x=>x.staff_id).map(x=>[String(x.staff_id).toUpperCase(),x]));let emps=await sbFetch('/rest/v1/employees?status=eq.AKTIF&select=employee_id,full_name,company_code,department,position,branch,status,salary_base,bank_name,bank_account&order=full_name.asc&limit=500');if(branchId==='HEAD_OFFICE'){emps=(emps||[]).filter(e=>['HEAD OFFICE','ADMIN'].includes(String(e.branch||'').toUpperCase()))}else if(branchId){const br=await sbFetch(`/rest/v1/branches?id=eq.${q(branchId)}&select=id,name,slug&limit=1`),b=br?.[0];if(!b)return{rows:[],summary:{count:0,total:0,late:0,leave:0},meta:{month:req.query.month,roster_source:'employees_active',missing_salary_count:0,kasbon_source:false,overtime_source:false}};emps=(emps||[]).filter(e=>{const k=String(e.employee_id||'').toUpperCase(),up=profileByCode[k];if(up?.branch_id)return String(up.branch_id)===branchId;const eb=String(e.branch||'').toLowerCase();return eb===String(b.slug||'').toLowerCase()||eb===String(b.name||'').toLowerCase()})}const empIds=[...new Set((emps||[]).map(x=>String(x.employee_id||'').toUpperCase()).filter(Boolean))];if(!empIds.length)return{rows:[],summary:{count:0,total:0,late:0,leave:0},meta:{month:req.query.month,roster_source:'employees_active',missing_salary_count:0,kasbon_source:false,overtime_source:false}};const snaps=await sbFetch(`/rest/v1/payroll?period_year=eq.${mr.year}&period_month=eq.${mr.month}&select=*`);const raos=await sbFetch(`/rest/v1/raos_payroll?effective_month=eq.${mr.start}&select=staff_id,gapok,bonus_saldo,bpjs,paket_data,member_parkir,bonus_kpi,target_pct,status_target,late_deduction_total,thp`);const att=await sbFetch(`/rest/v1/hris_attendance_view?date=gte.${mr.start}&date=lte.${mr.end}&select=employee_code,date,status,late_minutes,late_deduction_idr`);const leave=await sbFetch(`/rest/v1/leave_requests?start_date=lte.${mr.end}&end_date=gte.${mr.start}&status=eq.DISETUJUI&select=employee_id,total_days,leave_type,start_date,end_date`);const eMap=Object.fromEntries((emps||[]).map(x=>[String(x.employee_id).toUpperCase(),x]));const pMap=Object.fromEntries((snaps||[]).map(x=>[String(x.employee_id).toUpperCase(),x]));const codeByProfile=Object.fromEntries((profiles||[]).filter(x=>x.id&&x.staff_id).map(x=>[String(x.id).toUpperCase(),String(x.staff_id).toUpperCase()]));const rMap={};for(const x of (raos||[])){const raw=String(x.staff_id||'').toUpperCase(),code=codeByProfile[raw]||(eMap[raw]?raw:null);if(code)rMap[code]=x}const aMap={};for(const a of (att||[])){const k=String(a.employee_code||'').toUpperCase();if(!aMap[k])aMap[k]={present:0,late_minutes:0,late_deduction:0};if(String(a.status||'').toUpperCase()==='HADIR')aMap[k].present++;aMap[k].late_minutes+=Number(a.late_minutes||0);aMap[k].late_deduction+=Number(a.late_deduction_idr||0)}const lMap={};for(const l of (leave||[])){const k=String(l.employee_id||'').toUpperCase();lMap[k]=(lMap[k]||0)+Number(l.total_days||0)}const rows=(emps||[]).map(e=>{const k=String(e.employee_id||'').toUpperCase(),up=profileByCode[k]||{},snap=pMap[k]||null,r=rMap[k]||{},a=aMap[k]||{present:0,late_minutes:0,late_deduction:0};const frozen=!!snap&&['FINAL','PAID'].includes(String(snap.status||'').toUpperCase());const base=frozen?Number(snap.salary_base||0):Number(e.salary_base||up.gaji||r.gapok||0);const paketData=Number(r.paket_data||0),memberParkir=Number(r.member_parkir||0),bpjs=Number(r.bpjs||0);const liveAllowance=Number(r.bonus_saldo||0)+Number(r.bonus_kpi||0)+paketData+memberParkir+bpjs;const allowance=frozen?Number(snap.allowances||0):liveAllowance;const liveDeduction=Number(a.late_deduction||r.late_deduction_total||0);const deductions=frozen?Number(snap.deductions||0):liveDeduction;const total=frozen?Number(snap.total_salary||0):Math.max(0,base+allowance-deductions);return{id:snap?.id||null,employee_id:k,name:e.full_name||up.full_name||k,position:e.position||'',department:e.department||'',branch:e.branch||'',branch_id:up.branch_id||null,company_code:e.company_code||'RIFIM',salary_base:base,allowances:allowance,bonus_saldo:Number(r.bonus_saldo||0),bonus_kpi:Number(r.bonus_kpi||0),paket_data:paketData,member_parkir:memberParkir,bpjs,late_deduction:Number(a.late_deduction||r.late_deduction_total||0),deductions,total_salary:total,target_pct:Number(r.target_pct||0),target_status:r.status_target||'',attendance_days:a.present,late_minutes:a.late_minutes,leave_days:Number(lMap[k]||0),kasbon:0,overtime:0,document_number:snap?.document_number||'',status:snap?.status||'DRAFT',pdf_url:snap?.pdf_url||'',gdoc_url:snap?.gdoc_url||'',snapshot:frozen,bank_name:e.bank_name||'',bank_account:e.bank_account||''}}).sort((a,b)=>a.name.localeCompare(b.name));const missingSalaryCount=rows.filter(x=>!x.snapshot&&Number(x.salary_base||0)<=0).length;return{rows,summary:{count:rows.length,total:rows.reduce((s,x)=>s+x.total_salary,0),late:rows.reduce((s,x)=>s+x.late_deduction,0),leave:rows.reduce((s,x)=>s+x.leave_days,0)},meta:{month:req.query.month,roster_source:'employees_active',missing_salary_count:missingSalaryCount,kasbon_source:false,overtime_source:false}}}
async function ensureSoetaKpiScope(p,staffId){if(p.role!=='koordinator')return;const rows=await sbFetch(`/rest/v1/user_profiles?id=eq.${q(staffId)}&is_active=eq.true&select=id,branch_id&limit=1`),target=rows?.[0];if(!target?.branch_id||!p.branch_id||String(target.branch_id)!==String(p.branch_id))throw new Error('branch_out_of_scope')}
async function soetaKpiSnapshot(req,p){if(!canRead(p.role))throw new Error('Role tidak boleh akses SOETA KPI');const staffId=String(req.query.staff_id||'').trim();if(!/^[0-9a-f-]{36}$/i.test(staffId))throw new Error('staff_id UUID wajib');await ensureSoetaKpiScope(p,staffId);const month=monthStart(req.query.month);const snapshot=await sbFetch('/rest/v1/rpc/raos_soeta_kpi_staff_snapshot',{method:'POST',body:JSON.stringify({p_staff_id:staffId,p_month:month})});return{snapshot}}
async function soetaKpiManualUpsert(req,p){if(!canWrite(p.role))throw new Error('Hanya Admin/Direksi boleh mengubah SOETA KPI');const b=req.body||{},staffId=String(b.staff_id||'').trim(),month=monthStart(b.month);if(!/^[0-9a-f-]{36}$/i.test(staffId))throw new Error('staff_id UUID wajib');const score=name=>{const v=b[name];if(v==null||v==='')return null;const n=Number(v);if(!Number.isFinite(n)||n<0||n>100)throw new Error(`${name} wajib 0-100`);return n};const row={staff_id:staffId,effective_month:month,sop_score:score('sop_score'),coaching_score:score('coaching_score'),coordinator_score:score('coordinator_score'),notes:String(b.notes||'').trim()||null,updated_by:p.id,updated_at:new Date().toISOString()};const saved=await sbFetch('/rest/v1/raos_soeta_kpi_manual_inputs?on_conflict=staff_id,effective_month',{method:'POST',headers:{Prefer:'resolution=merge-duplicates,return=representation'},body:JSON.stringify(row)});return{row:saved?.[0]||row}}
function normEmail(v){return String(v||'').trim().toLowerCase()}
async function soetaReconciliation(p){if(!['admin','direksi','management'].includes(p.role))throw new Error('Role tidak boleh melihat reconciliation');const employees=await sbFetch('/rest/v1/employees?branch=ilike.*Soeta*&select=employee_id,full_name,email,phone,branch,position,status,company_code,join_date&order=full_name.asc&limit=500');const master=await sbFetch('/rest/v1/raos_staff_master?airport=eq.SOETA&select=staff_id,full_name,email,phone,terminal,role,status,is_activated,auth_user_id,branch_id&order=full_name.asc&limit=500');const byId=new Map((master||[]).map(x=>[String(x.staff_id||'').toUpperCase(),x]));const byEmail=new Map((master||[]).filter(x=>normEmail(x.email)).map(x=>[normEmail(x.email),x]));const rows=(employees||[]).map(e=>{let m=byId.get(String(e.employee_id||'').toUpperCase())||null,match_method=m?'employee_id':null;if(!m&&normEmail(e.email)){m=byEmail.get(normEmail(e.email))||null;if(m)match_method='email'}return{employee:e,master:m,matched:!!m,match_method,needs_terminal:!m?.branch_id,needs_auth:!m?.auth_user_id}});return{rows,summary:{employees:rows.length,matched:rows.filter(x=>x.matched).length,unmatched:rows.filter(x=>!x.matched).length,needs_terminal:rows.filter(x=>x.needs_terminal).length,needs_auth:rows.filter(x=>x.needs_auth).length},read_only:true}}
async function editAttendance(req,a){if(!canWrite(a.profile.role))throw new Error('Hanya Admin/Direksi boleh edit Absensi');const pub=env('SUPABASE_PUBLISHABLE_KEY'),url=env('SUPABASE_URL'),b=req.body||{};if(!b.id)throw new Error('id wajib');if(String(b.reason||'').trim().length<5)throw new Error('Alasan edit minimal 5 karakter');const r=await fetch(`${url}/rest/v1/rpc/hris_attendance_edit`,{method:'POST',headers:{apikey:pub,Authorization:a.bearer,'Content-Type':'application/json'},body:JSON.stringify({p_attendance_id:b.id,p_check_in_override:b.check_in_at_override||null,p_check_out_override:b.check_out_at_override||null,p_late_deduction_idr:b.late_deduction_idr==null?null:Number(b.late_deduction_idr),p_reason:String(b.reason).trim()})});const body=await read(r);if(!r.ok)throw new Error(body.message||'Gagal edit absensi');return body}
module.exports=async function handler(req,res){try{const a=await actor(req),mode=String(req.query.mode||req.body?.mode||'');if(req.method==='GET'&&mode==='branches')return out(res,200,{success:true,rows:await branches(a.profile),scope:a.profile.role==='koordinator'?'own_branch':'global'});if(req.method==='GET'&&mode==='employees')return out(res,200,{success:true,rows:await employees(req,a.profile),scope:a.profile.role==='koordinator'?'own_branch':'global'});if(req.method==='GET'&&mode==='preactivation')return out(res,200,{success:true,...await preactivation(a.profile)});if(req.method==='GET'&&mode==='attendance')return out(res,200,{success:true,rows:await attendance(req,a.profile),can_write:canWrite(a.profile.role)});if(req.method==='GET'&&mode==='payroll')return out(res,200,{success:true,...await payroll(req,a.profile),can_write:canWrite(a.profile.role)});if(req.method==='GET'&&mode==='soeta_kpi_snapshot')return out(res,200,{success:true,...await soetaKpiSnapshot(req,a.profile)});if(req.method==='GET'&&mode==='soeta_reconciliation')return out(res,200,{success:true,...await soetaReconciliation(a.profile)});if(req.method==='POST'&&mode==='soeta_kpi_manual_upsert')return out(res,200,{success:true,...await soetaKpiManualUpsert(req,a.profile)});if(req.method==='POST'&&mode==='attendance_edit')return out(res,200,{success:true,result:await editAttendance(req,a)});return out(res,404,{success:false,message:'Mode tidak dikenal'})}catch(e){return out(res,400,{success:false,message:e instanceof Error?e.message:String(e)})}}
