const PayrollModalV2=(()=>{function open(type,rows){if(!PayrollV2.canWrite())return alert('Hanya Admin/Direksi');const label=type==='kasbon'?'Kasbon Manual':'Deposit',wrap=document.createElement('div');wrap.className='modal-overlay open';wrap.innerHTML=`<div class="modal" style="max-width:480px"><div class="modal-header"><h3>${label}</h3><button class="modal-close">×</button></div><div class="modal-body"><div class="form-grid cols-1"><div class="form-group"><label>Karyawan</label><select id="pv2-emp">${rows.map(r=>`<option value="${r.employee_id}">${r.staff_name} — ${r.employee_id}</option>`).join('')}</select></div><div class="form-group"><label>Nominal</label><input id="pv2-amount" type="number" min="1"></div><div class="form-group"><label>Keterangan</label><textarea id="pv2-note" rows="2"></textarea></div></div></div><div class="modal-footer"><button class="btn btn-ghost pv2-cancel">Batal</button><button class="btn btn-primary pv2-save">Simpan</button></div></div>`;const close=()=>wrap.remove();wrap.querySelector('.modal-close').onclick=close;wrap.querySelector('.pv2-cancel').onclick=close;wrap.querySelector('.pv2-save').onclick=async()=>{const employee_id=wrap.querySelector('#pv2-emp').value,amount=Number(wrap.querySelector('#pv2-amount').value),note=wrap.querySelector('#pv2-note').value;if(!amount)return alert('Nominal wajib');const action=type==='kasbon'?'hris_payroll_v2_manual_kasbon':'hris_payroll_v2_deposit',res=await gasGet({action,employee_id,amount,note});if(res?.success){showToast(label+' tersimpan','success');close()}else alert(res?.message||'Gagal')};document.body.appendChild(wrap)}return{open}})();window.PayrollModalV2=PayrollModalV2;
