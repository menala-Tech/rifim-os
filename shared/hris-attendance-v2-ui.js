/** LOCAL ONLY — HRIS Attendance v2 UI policy. */
function hrisAttRole(){var r=String(currentUser&&currentUser.role||'').toUpperCase();return r==='DIREKTUR'?'DIREKSI':r}function hrisAttCanWrite(){return['ADMIN','DIREKSI'].includes(hrisAttRole())}function hrisAttCanRead(){return['ADMIN','DIREKSI','MANAGEMENT','KOORDINATOR'].includes(hrisAttRole())}function hrisAttCanExportPdf(){return['ADMIN','DIREKSI'].includes(hrisAttRole())}function hrisAttCanOvertime(){return['ADMIN','DIREKSI'].includes(hrisAttRole())}function hrisAttendanceBranchOptions(allBranches){var role=hrisAttRole();var rows=(allBranches||[]).filter(function(b){return b&&b.is_active!==false});if(['ADMIN','MANAGEMENT','DIREKSI'].includes(role)&&!rows.some(function(b){return String(b.name).toLowerCase()==='head office'}))rows.push({id:'HEAD_OFFICE',name:'Head Office',slug:'head-office',is_active:true,virtual:true});return rows}function validateAttendancePdfRange(from,to){if(!hrisAttCanExportPdf())return{ok:false,message:'Export PDF hanya untuk Admin/Direksi.'};if(!from||!to)return{ok:false,message:'Tanggal awal dan akhir wajib.'};var f=new Date(from+'T00:00:00'),t=new Date(to+'T00:00:00');if(f.getFullYear()!==t.getFullYear()||f.getMonth()!==t.getMonth())return{ok:false,message:'PDF hanya boleh satu bulan kalender.'};var last=new Date(f.getFullYear(),f.getMonth()+1,0).getDate();if(f.getDate()!==1||t.getDate()!==last)return{ok:false,message:'Rentang PDF wajib tanggal 01 sampai akhir bulan ('+String(last).padStart(2,'0')+').'};return{ok:true}}function attendanceActionCellV2(rowId){if(!hrisAttCanWrite())return'<span style="color:#888">Lihat saja</span>';return'<button class="btn btn-ghost btn-sm" data-att-write onclick="openAttendanceEditModal('+escAttr(JSON.stringify(String(rowId||'')))+')">'+iconSvg('pencil')+'</button>'}function applyAttendanceV2UiPolicy(){var manual=document.getElementById('btn-att-manual');if(manual)manual.style.display='none';var pdf=document.getElementById('btn-att-pdf');if(pdf)pdf.style.display=hrisAttCanExportPdf()?'':'none';var overtime=document.getElementById('btn-att-overtime');if(overtime)overtime.style.display=hrisAttCanOvertime()?'':'none';var branch=document.getElementById('filter-att-branch');if(branch&&hrisAttRole()==='KOORDINATOR'){branch.disabled=true;branch.title='Koordinator: cabang sendiri'}}
